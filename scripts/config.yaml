servers:
  tesla:
    host: "http://tesla.local"
    location: "tesla"
    runtimes:
      llama-server-nvidia:
        port: 8000
        token: ""
        models: ["Qwen3.5-27B-UD-Q4_K_XL", "Qwen3.6-35B-A3B-UD-Q4_K_S", "Qwen3.6-35B-A3B-UD-Q8_K_XL"]
      llama-server-amd:
        port: 8001
        token: ""
        models: ["Qwen3.6-35B-A3B-UD-IQ4_XS", "Qwen3.5-35B-A3B-UD-IQ4_XS"]
  macmini:
    host: "http://mac-mini.local"
    location: "mac-mini"
    runtimes:
      omlx:
        port: 8001
        token: "asdf"
        models: ["Qwen3.6-35B-A3B-4bit"]
      mlx-lm:
        port: 8080
        token: ""
        models: 
          - "mlx-community/Qwen3.5-35B-A3B-4bit"
          - "mlx-community/gemma-4-26b-a4b-it-4bit"
          - "mlx-community/Qwen3.6-35B-A3B-4bit" 
          - "mlx-community/gemma-4-e2b-it-4bit"
          - "mlx-community/gemma-4-e4b-it-4bit"
  # local:
  #   host: "http://localhost"
  #   location: "local"
  #   runtimes:
  #     llama-cpp:
  #       port: 8080
  #       token: "optional-token-here"
  #       models: ["custom-model-q4_k_m.gguf"]
  #     lm-studio:
  #       port: 1234
  #       token: "optional-token-here"
  #       models: ["model-loaded-in-ui"]

# Evaluator model used to judge quality benchmark responses.
# This model scores each fixture response against a rubric and is NOT itself
# a benchmark target — it must be a capable, instruction-following model
# (ideally larger or higher-quality than the models under test).
# The quality benchmark script reads this section separately from `servers`.
evaluator:
  host: "http://tesla.local"
  runtime: "llama-server-nvidia"
  port: 8000
  token: ""
  model: "gemma-4-31B-it-UD-Q8_K_XL"
  # temperature: 0.0 — evaluator always runs at zero temperature for determinism
  temperature: 0.0
  # max_tokens: budget for the evaluator's JSON verdict per fixture
  max_tokens: 512
